{
  "name": "pods",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pods-finance/$NAME",
    "configure": "tsc src/constants/addresses/$VARIANT.ts --outDir src/constants/_generated",
    "template": "mustache src/constants/_generated/$VARIANT.js subgraph.template.yaml > subgraph.yaml && mustache src/constants/_generated/$VARIANT.js src/constants/env.mustache > src/constants/env.ts",
    "prepare": "yarn configure && yarn template",
    "deploy:mainnet": "VARIANT=mainnet yarn prepare && NAME=pods yarn deploy",
    "deploy:kovan-dev": "VARIANT=kovan-dev yarn prepare && NAME=pods-kovan-dev yarn deploy",
    "deploy:kovan": "VARIANT=kovan yarn prepare && NAME=pods-kovan yarn deploy",
    "deploy:mumbai-dev": "VARIANT=mumbai-dev yarn prepare && NAME=pods-mumbai-dev yarn deploy",
    "deploy:mumbai": "VARIANT=mumbai yarn prepare && NAME=pods-mumbai yarn deploy",
    "deploy:matic": "VARIANT=matic yarn prepare && NAME=pods-matic yarn deploy",
    "tsc": "tsc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  },
  "devDependencies": {
    "mustache": "^4.2.0",
    "typescript": "^4.2.4"
  }
}